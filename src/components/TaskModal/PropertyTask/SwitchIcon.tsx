import { TypeOfProperty } from "@/models"

type Props = {
  type:TypeOfProperty
}
export const SwitchIcon = ({type}:Props) => {

  switch (type){
    case TypeOfProperty.ARCHIVE:
      return 
  }
  return (
    <div>

    </div>
  )
}